import { type Config } from "prettier";

const config: Config = {
  trailingComma: "all",
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  arrowParens: "always",
  printWidth: 80,
};

export default config;
